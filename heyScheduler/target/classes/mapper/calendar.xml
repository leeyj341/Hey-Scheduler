<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.project.heyScheduler.calendar">
	<select id="selectPlans" parameterType="plan" resultType="plan">
		<![CDATA[
			select * from plan where startdatetime >= #{startdatetime} and startdatetime <= #{enddatetime} order by startdatetime
		]]>
	</select>
	<select id="selectPlansOnDay" parameterType="plan" resultType="plan">
		<![CDATA[
			select * from plan where startdatetime >= #{startdatetime} and startdatetime < to_date(#{startdatetime}, 'yy-MM-dd') + 1 order by startdatetime
		]]>
	</select>
	<select id="selectPlanDetail" parameterType="String" resultType="plan">
		<![CDATA[
			select * from plan where plan_no = #{plan_no}
		]]>
	</select>
</mapper>